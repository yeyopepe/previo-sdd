---
name: pv-internal-doc-files
description: Shared, project-agnostic file-management procedure for the pv-* framework's three documentation folders (`docs.functional.featuresDocPathDir`, `docs.tech.architectureDocDir`, `docs.tech.styleBibleDocDir`) — one file per topic, each with a stable identifying number before the title and an `**Area**:` field, plus a generated `INDEX.md`. Offers two actions: `find` (locate whether a topic already has its own entry, before deciding whether to create a new one or edit the existing one) and `upsert` (write a topic's final file, already drafted by the caller, assigning a new number only if it's new, and regenerate the index). Internal use by the pv-internal-doc-features and pv-do skills.
user-invocable: false
model: claude-sonnet-5
effort: medium
metadata:
  version: 0.9.6b4
  uses: []
---

# pv-internal-doc-files

A single, shared procedure to organize any of the three documentation folders as one file per topic, instead of a single monolithic document — designed so that finding or updating one topic doesn't require reading the entire listing. Only invoked by `pv-internal-doc-features` (for `docs.functional.featuresDocPathDir`) and directly by `pv-do` (for `docs.tech.architectureDocDir`/`styleBibleDocDir`) — not meant for direct invocation by the user.

**This skill doesn't decide what the documentation says, nor how to write it.** It doesn't draft content, decide topics, or apply any writing style — that's always the caller's job (`pv-internal-doc-features` for its own domain rules, or `pv-do` directly guided by `pv-internal-doc-technical`/`pv-internal-doc-style` for architecture/style). This skill only knows **where** and **how** that already-drafted content should live once handed to it: naming the file, keeping the numbering and index consistent, and returning the relevant file when it needs checking whether it already exists.

**Language.** This skill doesn't talk to the user directly and doesn't decide any wording — it writes exactly the `title`/`area`/`body` strings the caller gives it, in whatever language the caller already chose. The only fixed-English parts are structural, not content: the `**Area**:` label and the `NNN` numeric prefix in the title, parsed literally by `rebuild-index.py`/`next-feature-number.py` regardless of `title`/`area`'s language.

## Folder convention

Given a `folder` (e.g. `docs/features`, `docs/architecture`, `docs/style`):

- `INDEX.md` — table of contents grouped by `**Area**:`, with links to each file. **Never edited by hand**: always regenerated by [`scripts/rebuild-index.py`](scripts/rebuild-index.py) from the rest of the files.
- One `{NNN}-{slug}.md` file per topic (flat, no subfolders per area; the filename carries the same number as the title, so the folder's file listing stays ordered the same as `INDEX.md`):
  - `# {NNN} — {title}` — the identifying number is stable: assigned once when the file is created (see the `upsert` action) and never changes again even if the title is edited later or another file is deleted (both in the title and the filename). It's for locating it quickly, not for ordering by relevance.
  - `**Area**: {area}`
  - The rest of the file is exactly the caller's `body`, verbatim — this skill doesn't parse, validate, or reformat it beyond the two lines above.
- Cross-links between files use the path relative to the destination file (`[text](NNN-other-slug.md)`), never `#` anchors — each topic lives in its own file. Building those links (if the caller's domain needs them) is the caller's responsibility, not this skill's.

## Expected input from the caller

The caller must give the `action` (`find` or `upsert`), the `folder` to operate on, and its own parameters (see below).

## Action `find`

Invoked by the caller before drafting, to know whether the topic it's about to document already has its own entry (so it can edit it in place) or is new.

Parameters: `folder`, a brief description of the topic to look for (approximate name, area, or what it's about).

1. If `folder` doesn't exist yet, there's no documented topic — return that there are no matches and stop here.
2. Read `INDEX.md` (if it doesn't exist but the folder does, regenerate it first with `scripts/rebuild-index.py` before reading it). It's a short listing — number, title and area, not the full content.
3. Judge by name/area whether any index entry semantically matches what the caller describes (no need for a literal match). If there are 1-2 plausible candidates, read those files in full (they're small) to confirm before responding.
4. Return to the caller: the path of the matching file (if any, with its current content already read) or that no equivalent entry exists yet.

## Action `upsert`

Invoked by the caller with the content already fully drafted (this skill doesn't rephrase anything).

Parameters:
- `folder`.
- `area` — text that should appear in `**Area**:` and group by in the index (functional area for features; technical/style topic for architecture/style).
- `title` — exactly as it should appear as `# ...`.
- `body` — the rest of the file's content, already fully drafted by the caller (for features: functional description + diagrams + `Available in`/`Code`/`Since`/`Last modified` already formatted; for architecture/style: the free-form technical content `pv-do` already drafts per `pv-internal-doc-technical`/`pv-internal-doc-style`). Written verbatim after the `**Area**:` line — this skill doesn't know or validate any domain field inside it.
- `existing_file` — path returned by a previous call to `find`, if this is an in-place edit; omitted if it's a new topic.

Steps:

1. If `folder` doesn't exist yet, create it.
2. **If there's an `existing_file`**: use that same filename (don't rename it even if the title changed slightly, so as not to break cross-links from other files already pointing to it) and **keep the identifying number** it already had in its original `#` (don't recompute it).
3. **If there's no `existing_file`** (new topic):
   - Compute the identifying number with `python .claude/skills/pv-internal-doc-files/scripts/next-feature-number.py --folder {folder}`.
   - Compute the title's slug with `python .claude/skills/pv-internal-doc-files/scripts/slugify.py "{title}"`.
   - The filename is `{number}-{slug}.md`.
4. Write (create or fully overwrite) `{folder}/{NNN}-{slug}.md`: first line `# {number} — {title}`, second line `**Area**: {area}`, then `{body}` as-is.
5. Run `python .claude/skills/pv-internal-doc-files/scripts/rebuild-index.py --folder {folder}` to regenerate `INDEX.md` deterministically from all the folder's files — don't edit `INDEX.md` by hand.
6. Return the written file's path to the caller.

## Assets and scripts

- [`scripts/_slug.py`](scripts/_slug.py) — shared internal helper, not directly invocable: `slugify()` normalizes a title into a lowercase ASCII slug, and `github_anchor()` replicates GitHub's anchor algorithm (used by `pv-internal-doc-features`'s `migrate-legacy-features-doc.py` to rewrite `#anchor` links when migrating a legacy `FEATURES.md`).
- [`scripts/next-feature-number.py`](scripts/next-feature-number.py) — computes the next free number (the title's prefix, not the filename) by finding the highest one already used in the folder; a deleted number is never reused.
- [`scripts/rebuild-index.py`](scripts/rebuild-index.py) — regenerates `INDEX.md` from every file in the folder, grouped by `**Area**:`; the sole source of truth for that index, never hand-edited.
- [`scripts/slugify.py`](scripts/slugify.py) — computes the text part (slug) of a new file's filename (`{number}-{slug}.md`); the number already guarantees no collision, so the slug doesn't need to check anything on its own.
